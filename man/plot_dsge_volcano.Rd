% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsge.R
\name{plot_dsge_volcano}
\alias{plot_dsge_volcano}
\title{Gene-level volcano plot for a specific pathway}
\usage{
plot_dsge_volcano(
  de_results,
  dsge_result = NULL,
  pathway_genes,
  go_id,
  logFC_col = "log2FoldChange",
  pval_col = "pvalue",
  padj_col = NULL,
  gene_col = "geneName",
  gene_id_col = "db_object_symbol",
  threshold = 0.05,
  padj_threshold = 0.05,
  lfc_threshold = NULL,
  color_up = "#CC3333",
  color_down = "#3366CC",
  color_nom = "#CC9933",
  color_ns = "#AAAAAA",
  alpha_sig = 0.9,
  alpha_ns = 0.5,
  label = NULL,
  label_genes = NULL,
  label_sig = FALSE,
  cex_label = 0.7,
  cex_point = 1.6,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  go_name = NULL,
  ...
)
}
\arguments{
\item{de_results}{Data.frame of differential expression results. Must
contain at least log2FC, p-value, and gene identifier columns.}

\item{dsge_result}{Optional result from \code{\link{pathway_dsge}()}.
Pass either the full list (where \code{dsge_result$table} is used)
or the result data.frame directly. When provided, the pathway's
\code{dsge_std} and \code{p_adj} are shown in the annotation.
Default \code{NULL}.}

\item{pathway_genes}{A named list of pathway-gene mappings, as used in
\code{\link{pathway_dsge}()}. Each element is a \code{data.frame}
with a gene identifier column.}

\item{go_id}{A single character string specifying which pathway (GO ID)
to plot. Must be a name in \code{pathway_genes}.}

\item{logFC_col}{Column name in \code{de_results} for log2 fold change.
Default \code{"log2FoldChange"}.}

\item{pval_col}{Column name in \code{de_results} for the p-value
(raw or adjusted). Default \code{"pvalue"}.}

\item{padj_col}{Optional column name in \code{de_results} for adjusted
p-values (e.g. \code{"padj"}). When provided, points are classified
as adjusted-significant using \code{padj_threshold}, and an
additional "Nominal" category appears in the legend. Default
\code{NULL} (uses \code{threshold} only).}

\item{gene_col}{Column name in \code{de_results} for gene identifiers
that match the pathway mapping. Default \code{"geneName"}.}

\item{gene_id_col}{Column name in \code{pathway_genes[[go_id]]} that
holds the gene identifiers. Default \code{"db_object_symbol"}.}

\item{threshold}{p-value significance threshold for the horizontal
reference line. Default \code{0.05}.}

\item{padj_threshold}{Adjusted p-value threshold for significance
classification. Default \code{0.05}. Only used when
\code{padj_col} is provided.}

\item{lfc_threshold}{Numeric vector of logFC thresholds for vertical
reference lines (e.g. \code{c(-1, 1)}). Default \code{NULL}.}

\item{color_up}{Color for significantly up-regulated genes.
Default \code{"#CC3333"}.}

\item{color_down}{Color for significantly down-regulated genes.
Default \code{"#3366CC"}.}

\item{color_nom}{Color for nominally significant genes (raw p-value
below \code{threshold} but adjusted p-value above
\code{padj_threshold}). Default \code{"#CC9933"}. Only used when
\code{padj_col} is provided.}

\item{color_ns}{Color for non-significant genes.
Default \code{"#AAAAAA"}.}

\item{alpha_sig}{Transparency for significant points.
Default \code{0.9}.}

\item{alpha_ns}{Transparency for non-significant points.
Default \code{0.5}.}

\item{label}{Whether to label genes with their names. Auto-set to
\code{TRUE} when the pathway has \eqn{\le 80} matched genes,
\code{FALSE} otherwise. Can be forced with \code{TRUE} or
\code{FALSE}.}

\item{label_genes}{Optional character vector of specific gene names
within the pathway to label. Overrides \code{label}. Useful for
very large pathways where you only want to highlight a few key genes.}

\item{label_sig}{When \code{TRUE}, only label genes that pass the
\code{threshold}. Default \code{FALSE}. Ignored when
\code{label_genes} is provided.}

\item{cex_label}{Text size for gene labels. Default \code{0.70}.}

\item{cex_point}{Point size for significant genes. Non-significant
genes are plotted at \code{0.7 * cex_point}. Default \code{1.6}.}

\item{xlab, ylab}{Axis labels. Auto-generated when \code{NULL}.}

\item{main}{Plot title. Default \code{NULL}, auto-generated from
GO ID and GO name.}

\item{go_name}{Optional GO term name for the title. When \code{NULL},
the function looks for a \code{go_name} column in the DSGE result
table or uses only the GO ID.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}()}.}
}
\value{
Invisibly returns the subset of \code{de_results} for the
  pathway genes (data.frame).
}
\description{
Plots a focused volcano plot showing only the genes belonging to a
specified GO pathway. Each point is one gene from the pathway, with
log2 fold change on the x-axis and statistical significance (-log10
p-value) on the y-axis. This allows visual inspection of the direction,
magnitude, and distribution of perturbation within a single pathway.
}
\examples{
\donttest{
# Build input objects with simulated data for demonstration
set.seed(42)
de_results <- data.frame(
  log2FoldChange = rnorm(100, mean = 0, sd = 1.5),
  pvalue = runif(100),
  geneName = paste0("GENE", 1:100),
  stringsAsFactors = FALSE
)
pw <- list(
  "GO:0042101" = data.frame(
    db_object_symbol = paste0("GENE", 1:15),
    go_name = "T cell receptor complex",
    stringsAsFactors = FALSE
  )
)
dsge <- pathway_dsge(pw, de_results$pvalue, de_results$pvalue,
                     de_results$geneName, min_size = 1, n_perm = 100,
                     seed = 42)
plot_dsge_volcano(de_results, dsge, pw, go_id = "GO:0042101",
  logFC_col = "log2FoldChange", pval_col = "pvalue", gene_col = "geneName")
}
}
