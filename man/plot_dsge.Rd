% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsge.R
\name{plot_dsge}
\alias{plot_dsge}
\title{Plot null distribution vs. observed DSGE for selected pathways}
\usage{
plot_dsge(
  result,
  n = 9L,
  pathway_ids = NULL,
  go_ids = NULL,
  col_null = "#2166AC",
  col_tail = "#E41A1C",
  col_obs = "#D73027",
  col_thr = "#999999",
  safety_margin = NULL,
  cex_main = 0.85,
  use_std = NULL
)
}
\arguments{
\item{result}{List returned by \code{\link{pathway_dsge}()} with
\code{return_null = TRUE} (contains \code{table}, \code{null_raw},
\code{null_gpd}).}

\item{n}{When \code{pathway_ids = NULL} and \code{go_ids = NULL},
the top \code{n} most significant pathways (by p_adj ascending) are
plotted. Default 9. Ignored when \code{pathway_ids} or
\code{go_ids} is specified.}

\item{pathway_ids}{Optional character vector of pathway IDs to plot
(e.g. GO IDs, KEGG IDs, or Reactome IDs depending on source). When
provided, directly plots these pathways, overriding \code{n}.
Unmatched IDs are skipped with a warning.}

\item{go_ids}{\strong{[Deprecated]}. Use \code{pathway_ids} instead.
Optional character vector of GO term IDs to plot
(e.g. \code{c("GO:0007156", "GO:0007268")}). When provided,
directly plots these pathways, overriding \code{n}. Unmatched IDs
are skipped with a warning.}

\item{col_null}{Color of the null distribution density curve.
Default \code{"#2166AC"}.}

\item{col_tail}{Color of the GPD tail region highlight.
Default \code{"#E41A1C"}.}

\item{col_obs}{Color of the observed DSGE vertical line.
Default \code{"#D73027"}.}

\item{col_thr}{Color of the GPD threshold vertical line.
Default \code{"#999999"}.}

\item{safety_margin}{Safety margin for visual GPD tail extension
in the plot. Defaults to the value from \code{pathway_dsge()} result.}

\item{cex_main}{Title font scaling. Default \code{0.85}.}

\item{use_std}{Whether to plot standardised DSGE. Defaults to
\code{TRUE} when the result table contains a \code{dsge_std}
column (i.e. \code{pathway_dsge()} was run with
\code{use_std = TRUE}), and \code{FALSE} otherwise.}
}
\value{
No return value; called for its side effect (plotting).
}
\description{
For pathways in a \code{\link{pathway_dsge}()} result, plots the
density of the permutation null distribution with a dashed red line
marking the observed DSGE. If GPD tail fitting is available for the
size group, the tail region is highlighted in semi-transparent orange.
}
\examples{
\dontrun{
result <- pathway_dsge(pw, res$pvalue, res$baseMean, res$geneName,
                       seed = 42, return_null = TRUE, heterogeneity = TRUE)
# Top 9
plot_dsge(result, n = 9)
# Selected pathways
plot_dsge(result, pathway_ids = c("GO:0007156", "GO:0007268"))
# Standardised scale
plot_dsge(result, n = 9, use_std = TRUE)
}
}
